{
  "$defs": {
    "BatchJobDesign": {
      "description": "One program's Spring Batch job design -- one per Track C program, LLM-authored.",
      "properties": {
        "domain_entities": {
          "items": {
            "type": "string"
          },
          "title": "Domain Entities",
          "type": "array"
        },
        "job_name": {
          "title": "Job Name",
          "type": "string"
        },
        "job_parameters": {
          "default": [],
          "items": {
            "$ref": "#/$defs/JobParameter"
          },
          "title": "Job Parameters",
          "type": "array"
        },
        "program_name": {
          "title": "Program Name",
          "type": "string"
        },
        "steps": {
          "items": {
            "$ref": "#/$defs/BatchStepDesign"
          },
          "title": "Steps",
          "type": "array"
        }
      },
      "required": [
        "program_name",
        "job_name",
        "domain_entities",
        "steps"
      ],
      "title": "BatchJobDesign",
      "type": "object"
    },
    "BatchStepDesign": {
      "description": "One step of a `BatchJobDesign`, LLM-authored (ADR-0010 decision 3).\n\n`source_paragraphs` names the real COBOL paragraph(s) (from `SpecExtractionResult.paragraph_names`)\nthis step's logic comes from -- provenance one hop further than `spec_extractor`'s own\nsource-label tracing (ADR-0006), now into the batch design itself.",
      "properties": {
        "control_break": {
          "anyOf": [
            {
              "$ref": "#/$defs/ControlBreakDesign"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "description": {
          "title": "Description",
          "type": "string"
        },
        "guard_condition": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "title": "Guard Condition"
        },
        "input_type": {
          "title": "Input Type",
          "type": "string"
        },
        "job_parameters": {
          "default": [],
          "items": {
            "type": "string"
          },
          "title": "Job Parameters",
          "type": "array"
        },
        "optional_lookups": {
          "default": [],
          "items": {
            "type": "string"
          },
          "title": "Optional Lookups",
          "type": "array"
        },
        "output_type": {
          "title": "Output Type",
          "type": "string"
        },
        "reads_own_writes": {
          "default": false,
          "title": "Reads Own Writes",
          "type": "boolean"
        },
        "role": {
          "enum": [
            "reader",
            "processor",
            "writer",
            "tasklet"
          ],
          "title": "Role",
          "type": "string"
        },
        "source_paragraphs": {
          "items": {
            "type": "string"
          },
          "title": "Source Paragraphs",
          "type": "array"
        },
        "step_name": {
          "description": "A camelCase Java identifier, e.g. 'computeMonthlyInterest'. A class name is derived from this directly, so it must also not be a Java reserved word.",
          "pattern": "^[A-Za-z_$][A-Za-z0-9_$]*$",
          "title": "Step Name",
          "type": "string"
        }
      },
      "required": [
        "step_name",
        "source_paragraphs",
        "role",
        "description",
        "input_type",
        "output_type",
        "guard_condition"
      ],
      "title": "BatchStepDesign",
      "type": "object"
    },
    "ComplexityTier": {
      "description": "Which routing band a unit of work falls into.\n\nOrdered cheapest to most capable. `COMPLEX` is the safe default for any caller that cannot\ncompute signals (see `core.model_routing.resolve_routing`) -- being wrong toward *more*\ncapability costs money, being wrong toward less costs correctness, and this repo's whole\nposture is that a plausible-but-wrong answer is the worst outcome available.",
      "enum": [
        "simple",
        "moderate",
        "complex"
      ],
      "title": "ComplexityTier",
      "type": "string"
    },
    "CompositeComponent": {
      "description": "One component of a `CompositeType`: a field name, and the entity it holds.",
      "properties": {
        "entity_name": {
          "title": "Entity Name",
          "type": "string"
        },
        "field_name": {
          "title": "Field Name",
          "type": "string"
        }
      },
      "required": [
        "field_name",
        "entity_name"
      ],
      "title": "CompositeComponent",
      "type": "object"
    },
    "CompositeType": {
      "description": "A record composed of existing domain entities, for a value no single copybook describes.\n\nIntroduced by ADR-0020 because a real `solution_architect` run chains processor steps, and the\nvalues flowing between them are not entities: \"a `TranCatBal` with its `Account` and `CardXref`\nresolved\" is a type the target genuinely needs and no copybook declares.\n\n**Every component references an entity that already exists.** A composite never introduces a\nfield a copybook did not produce, so `pic_mapper`'s computed precision and scale still reach the\ngenerated code unchanged -- the composite only says which records travel together.\n\nRendered deterministically rather than generated (ADR-0010's line, unmoved): the *shape* is a\nmechanical transform of this declaration, and only the *name* is judgment.",
      "properties": {
        "components": {
          "items": {
            "$ref": "#/$defs/CompositeComponent"
          },
          "title": "Components",
          "type": "array"
        },
        "name": {
          "title": "Name",
          "type": "string"
        }
      },
      "required": [
        "name",
        "components"
      ],
      "title": "CompositeType",
      "type": "object"
    },
    "ControlBreakDesign": {
      "description": "What a step groups by and what it accumulates -- the fact a control break hides in an idiom.\n\n**Why it is on the step rather than in the job.** A control break performs one paragraph at the\ngroup boundary, and that paragraph is what a step declares in `source_paragraphs`. Attaching it\nanywhere else would leave a renderer matching paragraph names to find it.\n\n**`accumulated_from_field` is what to sum, and `landing_field` is where a generated type can\nfind it.** `WS-TOTAL-INT` accumulates `WS-MONTHLY-INT`, which is also moved to `TRAN-AMT`. The\naccumulator is a program variable no generated record has; the landing field is a column an\naggregation can actually sum, which is what makes ADR-0027's \"already-summed item\" renderable\nrather than a note.\n\nDeterministic throughout: every field is read from the COBOL by\n`parsing/control_break.py`, and a program whose idiom does not match completely gets no\n`ControlBreakDesign` at all rather than a partial one.",
      "properties": {
        "accumulated_from_field": {
          "title": "Accumulated From Field",
          "type": "string"
        },
        "accumulator_field": {
          "title": "Accumulator Field",
          "type": "string"
        },
        "add_line": {
          "title": "Add Line",
          "type": "integer"
        },
        "break_key_field": {
          "title": "Break Key Field",
          "type": "string"
        },
        "landing_field": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Landing Field"
        },
        "performed_paragraph": {
          "title": "Performed Paragraph",
          "type": "string"
        },
        "test_line": {
          "title": "Test Line",
          "type": "integer"
        }
      },
      "required": [
        "break_key_field",
        "accumulator_field",
        "accumulated_from_field",
        "performed_paragraph",
        "test_line",
        "add_line"
      ],
      "title": "ControlBreakDesign",
      "type": "object"
    },
    "DomainEntity": {
      "description": "One unified domain entity -- every program that `COPY`s the same copybook shares one.\n\n`source_copybook` is the exact copybook name that produced this entity; two different\ncopybook names are never merged into one entity, even when structurally similar (ADR-0010\ndecision 1) -- `source_copybook` is what makes that traceable and checkable.",
      "properties": {
        "fields": {
          "items": {
            "$ref": "#/$defs/DomainField"
          },
          "title": "Fields",
          "type": "array"
        },
        "name": {
          "title": "Name",
          "type": "string"
        },
        "record_length": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Record Length"
        },
        "source_copybook": {
          "title": "Source Copybook",
          "type": "string"
        },
        "used_by_programs": {
          "items": {
            "type": "string"
          },
          "title": "Used By Programs",
          "type": "array"
        }
      },
      "required": [
        "name",
        "source_copybook",
        "used_by_programs",
        "fields"
      ],
      "title": "DomainEntity",
      "type": "object"
    },
    "DomainField": {
      "description": "One field of a `DomainEntity`, reusing `pic_mapper`'s deterministic output verbatim.\n\n`java_field_name` and the entity's own `name` are direct, mechanical transforms of the COBOL\nname (kebab-case to camelCase/PascalCase, `-RECORD` suffix stripped from the entity name) --\nnot a business-semantic rename. See ADR-0010 decision 2 for why: a human-readable name is a\njudgment call for the LLM-authored parts of `UnifiedDesign` to make, not this deterministic\nfield.",
      "properties": {
        "byte_offset": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Byte Offset"
        },
        "cobol_field_name": {
          "title": "Cobol Field Name",
          "type": "string"
        },
        "java_field_name": {
          "title": "Java Field Name",
          "type": "string"
        },
        "java_type": {
          "title": "Java Type",
          "type": "string"
        },
        "length": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Length"
        },
        "precision": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "title": "Precision"
        },
        "scale": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "title": "Scale"
        },
        "signed": {
          "title": "Signed",
          "type": "boolean"
        }
      },
      "required": [
        "java_field_name",
        "cobol_field_name",
        "java_type",
        "precision",
        "scale",
        "signed"
      ],
      "title": "DomainField",
      "type": "object"
    },
    "FileAccessPath": {
      "description": "How one program reaches one domain entity's data -- the fact G31 found missing.\n\n**Why this exists.** ADR-0030 established that a reader cannot be rendered from the design as it\nstood: `CompositeType` declares that `TranCatBalWithRate` is composed of four entities, and says\nnothing about which of them is a stream, which are keyed lookups, or what the keys are. The\nCOBOL says all of it -- `FILE-CONTROL` for the declaration, `READ ... INTO` for which record a\nfile yields -- and `parsing/file_control.py` reads both. This is where that lands so a renderer\ncan use it.\n\n**Per program, not per entity, and that is measured rather than stylistic.** `TCATBAL` is\n`ACCESS MODE IS SEQUENTIAL` in `CBACT04C` and `RANDOM` in `CBTRN02C`; both are true, and an\naccess path recorded on `DomainEntity` would have to pick one and be wrong for the other.\n\n**`effective_key` is the one derived field, and it is derived because the two sources disagree\non purpose.** `CBACT04C` declares `XREF-FILE` with `RECORD KEY IS FD-XREF-CARD-NUM` and reads it\n`KEY IS FD-XREF-ACCT-ID` -- the alternate. The declaration says which keys the file supports; the\nread says which one this program positions on. A renderer that took the declared key would\ncompile and find nothing, so the field that answers *\"what do I look this up by\"* is the read's\nkey when it names one and the declared record key otherwise.",
      "properties": {
        "access_mode": {
          "title": "Access Mode",
          "type": "string"
        },
        "alternate_record_keys": {
          "default": [],
          "items": {
            "type": "string"
          },
          "title": "Alternate Record Keys",
          "type": "array"
        },
        "assign_to": {
          "title": "Assign To",
          "type": "string"
        },
        "declared_record_key": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Declared Record Key"
        },
        "effective_key": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Effective Key"
        },
        "entity_name": {
          "title": "Entity Name",
          "type": "string"
        },
        "is_keyed_lookup": {
          "default": false,
          "title": "Is Keyed Lookup",
          "type": "boolean"
        },
        "key_parts": {
          "default": [],
          "items": {
            "$ref": "#/$defs/LookupKeyPart"
          },
          "title": "Key Parts",
          "type": "array"
        },
        "organization": {
          "title": "Organization",
          "type": "string"
        },
        "program_name": {
          "title": "Program Name",
          "type": "string"
        },
        "read_line": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Read Line"
        },
        "record_name": {
          "title": "Record Name",
          "type": "string"
        },
        "select_line": {
          "title": "Select Line",
          "type": "integer"
        },
        "select_name": {
          "title": "Select Name",
          "type": "string"
        },
        "write_lines": {
          "default": [],
          "items": {
            "type": "integer"
          },
          "title": "Write Lines",
          "type": "array"
        },
        "write_mode": {
          "anyOf": [
            {
              "enum": [
                "append",
                "replace",
                "upsert"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Write Mode"
        },
        "written_entity_name": {
          "default": "",
          "title": "Written Entity Name",
          "type": "string"
        }
      },
      "required": [
        "program_name",
        "entity_name",
        "record_name",
        "select_name",
        "assign_to",
        "organization",
        "access_mode",
        "select_line"
      ],
      "title": "FileAccessPath",
      "type": "object"
    },
    "GateItem": {
      "description": "One fact a human (via control-plane's gate) should look at before approving `design.json`.\n\n`summary` is a short, skimmable one-liner; `detail` carries the full context (the original\nexception message, fidelity-check finding, rationale, or matched text) -- a reviewer scanning\n`gate_items` reads summaries first, then opens `detail` on whichever ones warrant it.",
      "properties": {
        "category": {
          "enum": [
            "unsupported_construct",
            "fidelity_issue",
            "low_confidence_rule",
            "injection_flag"
          ],
          "title": "Category",
          "type": "string"
        },
        "detail": {
          "title": "Detail",
          "type": "string"
        },
        "program_name": {
          "title": "Program Name",
          "type": "string"
        },
        "summary": {
          "title": "Summary",
          "type": "string"
        }
      },
      "required": [
        "category",
        "program_name",
        "summary",
        "detail"
      ],
      "title": "GateItem",
      "type": "object"
    },
    "InjectionFlag": {
      "description": "One best-effort heuristic match of a classic prompt-injection phrasing.\n\nThis is defense-in-depth, not a guarantee -- see the module docstring. `pattern` names which\nheuristic matched (not a specific regex, which is an implementation detail); `matched_text`\nand `line_number` are provided so a caller (or a human reviewing a flagged gate item) can see\nexactly what triggered the flag without re-scanning the source themselves.",
      "properties": {
        "line_number": {
          "title": "Line Number",
          "type": "integer"
        },
        "matched_text": {
          "title": "Matched Text",
          "type": "string"
        },
        "pattern": {
          "title": "Pattern",
          "type": "string"
        }
      },
      "required": [
        "pattern",
        "matched_text",
        "line_number"
      ],
      "title": "InjectionFlag",
      "type": "object"
    },
    "JobParameter": {
      "description": "One property of an invocation rather than of an item (ADR-0026, gap G29).\n\n**Why this is a declared fact and not something a body reads.** A stateless `ItemProcessor` has\nno access to when it ran or what it was invoked with, and a real model given no alternative\nreached for `LocalDateTime.now()` -- which compiles, reads correctly, and makes the same input\nproduce a different record on every run. `NonDeterministicBodyError` refuses that; this is what\nit refuses *in favour of*.\n\n`source_cobol` names where the value comes from in the program, so a reviewer can check the\ndeclaration against the source rather than trust it. `CBACT04C`'s `PARM-DATE` is the\nunambiguous case: it is in the `LINKAGE SECTION` and arrives via\n`PROCEDURE DIVISION USING EXTERNAL-PARMS`, so it is a job parameter in the COBOL too.",
      "properties": {
        "description": {
          "title": "Description",
          "type": "string"
        },
        "java_type": {
          "title": "Java Type",
          "type": "string"
        },
        "name": {
          "title": "Name",
          "type": "string"
        },
        "source_cobol": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Source Cobol"
        }
      },
      "required": [
        "name",
        "java_type",
        "description"
      ],
      "title": "JobParameter",
      "type": "object"
    },
    "LookupKeyPart": {
      "description": "One component of a keyed lookup's key, and where its value comes from.\n\n**The join predicate, read from the COBOL rather than declared by anyone.** `FILE-CONTROL` says\n`ACCOUNT-FILE` is read by `FD-ACCT-ID`; `MOVE TRANCAT-ACCT-ID TO FD-ACCT-ID` says what goes in\nit. ADR-0030 refused an LLM-declared join because a wrong one produces plausible rows and a\nsilently wrong comparison -- and it turns out nobody needs to declare it, because the program\nalready does.\n\nA key can have several parts: `DISCGRP-FILE`'s is a group of three, filled by three separate\n`MOVE`s.\n\n`literal` is set instead of `source_field` when the program moves a constant. That is not an\nedge case here -- it is how `1200-GET-INTEREST-RATE` retries under `'DEFAULT'`, which is finding\nF4's \"business logic living in wiring\" arriving as a fact a renderer can carry.",
      "properties": {
        "is_fallback": {
          "default": false,
          "title": "Is Fallback",
          "type": "boolean"
        },
        "key_field": {
          "title": "Key Field",
          "type": "string"
        },
        "key_offset": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Key Offset"
        },
        "key_width": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Key Width"
        },
        "literal": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Literal"
        },
        "source_field": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Source Field"
        },
        "source_line": {
          "title": "Source Line",
          "type": "integer"
        }
      },
      "required": [
        "key_field",
        "source_line"
      ],
      "title": "LookupKeyPart",
      "type": "object"
    },
    "PicFieldType": {
      "description": "What kind of Java shape a PIC clause maps to.",
      "enum": [
        "numeric",
        "alphanumeric"
      ],
      "title": "PicFieldType",
      "type": "string"
    },
    "PicMapping": {
      "description": "Deterministic result of mapping one COBOL field declaration's PIC clause.\n\nFor `field_type == ALPHANUMERIC`, `precision`/`scale` are `None` and `java_type` is\n`\"String\"` -- callers must not treat an alphanumeric field as a `BigDecimal` candidate.",
      "properties": {
        "field_name": {
          "title": "Field Name",
          "type": "string"
        },
        "field_type": {
          "$ref": "#/$defs/PicFieldType"
        },
        "java_type": {
          "title": "Java Type",
          "type": "string"
        },
        "precision": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Precision"
        },
        "raw_pic": {
          "title": "Raw Pic",
          "type": "string"
        },
        "scale": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Scale"
        },
        "signed": {
          "default": false,
          "title": "Signed",
          "type": "boolean"
        },
        "source_text": {
          "title": "Source Text",
          "type": "string"
        },
        "string_length": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "String Length"
        },
        "usage": {
          "$ref": "#/$defs/UsageClause",
          "default": "DISPLAY"
        }
      },
      "required": [
        "field_name",
        "raw_pic",
        "field_type",
        "java_type",
        "source_text"
      ],
      "title": "PicMapping",
      "type": "object"
    },
    "ProgramComplexity": {
      "description": "The measured signals and the tier they produce, kept together so a decision is explainable.\n\nCarried rather than discarded because `tier` alone cannot answer \"why was this program routed\nto the cheap model?\" at a review gate -- the signals are the answer, and they are free to keep.",
      "properties": {
        "copybook_count": {
          "title": "Copybook Count",
          "type": "integer"
        },
        "mapped_field_count": {
          "title": "Mapped Field Count",
          "type": "integer"
        },
        "paragraph_count": {
          "title": "Paragraph Count",
          "type": "integer"
        },
        "program_name": {
          "title": "Program Name",
          "type": "string"
        },
        "prompt_chars": {
          "title": "Prompt Chars",
          "type": "integer"
        },
        "rationale": {
          "title": "Rationale",
          "type": "string"
        },
        "tier": {
          "$ref": "#/$defs/ComplexityTier"
        },
        "unsupported_field_count": {
          "title": "Unsupported Field Count",
          "type": "integer"
        }
      },
      "required": [
        "program_name",
        "prompt_chars",
        "paragraph_count",
        "mapped_field_count",
        "unsupported_field_count",
        "copybook_count",
        "tier",
        "rationale"
      ],
      "title": "ProgramComplexity",
      "type": "object"
    },
    "ProgramDesignEntry": {
      "description": "One program's full `spec_extractor` + `spec_critic` output, as it goes into `design.json`.",
      "properties": {
        "critique": {
          "$ref": "#/$defs/SpecCritiqueResult"
        },
        "program_name": {
          "title": "Program Name",
          "type": "string"
        },
        "spec_extraction": {
          "$ref": "#/$defs/SpecExtractionResult"
        }
      },
      "required": [
        "program_name",
        "spec_extraction",
        "critique"
      ],
      "title": "ProgramDesignEntry",
      "type": "object"
    },
    "RestEndpointDesign": {
      "description": "One thin-REST-layer endpoint, LLM-authored -- control/observability or a point query.\n\nPer ADR-0009, this layer triggers/monitors Spring Batch jobs and serves genuine point queries\nagainst `domain_entities` -- never the bulk batch logic itself.",
      "properties": {
        "description": {
          "title": "Description",
          "type": "string"
        },
        "domain_entity": {
          "title": "Domain Entity",
          "type": "string"
        },
        "method": {
          "enum": [
            "GET",
            "POST",
            "PUT",
            "DELETE"
          ],
          "title": "Method",
          "type": "string"
        },
        "path": {
          "title": "Path",
          "type": "string"
        }
      },
      "required": [
        "method",
        "path",
        "domain_entity",
        "description"
      ],
      "title": "RestEndpointDesign",
      "type": "object"
    },
    "RuleConfidence": {
      "description": "One claim from `spec.md`, independently scored against the real COBOL source.\n\n`rule` is a self-contained restatement of the claim (not just \"see spec.md line N\") so it's\nidentifiable without cross-referencing the narration again. `confidence` is in `[0.0, 1.0]`.",
      "properties": {
        "confidence": {
          "title": "Confidence",
          "type": "number"
        },
        "rationale": {
          "title": "Rationale",
          "type": "string"
        },
        "rule": {
          "title": "Rule",
          "type": "string"
        }
      },
      "required": [
        "rule",
        "confidence",
        "rationale"
      ],
      "title": "RuleConfidence",
      "type": "object"
    },
    "RunCost": {
      "description": "What one `design` invocation actually consumed, for the reviewing gate to see (ADR-0018).\n\nActuals, summed from real `ModelCallResult`s -- deliberately not\n`RoutingDecision.estimated_cost_usd`, which is what *selection predicted* from a measured\ntoken profile before any call ran. Both are worth having and the gap between them is worth\nsurfacing, but conflating them in one field would make the number unfalsifiable. Estimated\ncost is not carried here yet; that is a follow-up, named rather than half-built.\n\n`notional_cost_usd` is `None` when no backend reported a cost, and is a **partial** sum when\n`calls_without_reported_cost > 0` -- read the two together or not at all. The SDK backend\nreports none by design (`core/model_client.py` keeps no rate card, so it cannot go stale), so\non that backend the token counts are the real signal and the dollar figure is absent rather\nthan wrong. On a subscription the CLI's own `total_cost_usd` is notional in the first place:\nit is what the call *would* cost at API rates, not what anyone was billed.",
      "properties": {
        "cache_creation_input_tokens": {
          "title": "Cache Creation Input Tokens",
          "type": "integer"
        },
        "cache_read_input_tokens": {
          "title": "Cache Read Input Tokens",
          "type": "integer"
        },
        "calls_without_reported_cost": {
          "default": 0,
          "title": "Calls Without Reported Cost",
          "type": "integer"
        },
        "input_tokens": {
          "title": "Input Tokens",
          "type": "integer"
        },
        "model_calls": {
          "title": "Model Calls",
          "type": "integer"
        },
        "notional_cost_usd": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Notional Cost Usd"
        },
        "output_tokens": {
          "title": "Output Tokens",
          "type": "integer"
        }
      },
      "required": [
        "model_calls",
        "input_tokens",
        "output_tokens",
        "cache_creation_input_tokens",
        "cache_read_input_tokens"
      ],
      "title": "RunCost",
      "type": "object"
    },
    "SpecCritiqueResult": {
      "description": "Everything `spec_critic` produces for one program's `spec_extractor` output.\n\n`overall_confidence` is `0.0` whenever `fidelity_issues` is non-empty, regardless of\n`rule_confidence` -- see the module docstring's decision (1). Otherwise it is the minimum of\n`rule_confidence`'s scores (decision (2)), or `1.0` if the critic model found no checkable\nrules at all (an empty spec has nothing to be unconfident about, which is itself something\n`fidelity_issues`' paragraph-coverage check would already have caught if it were a defect).",
      "properties": {
        "fidelity_issues": {
          "items": {
            "type": "string"
          },
          "title": "Fidelity Issues",
          "type": "array"
        },
        "overall_confidence": {
          "title": "Overall Confidence",
          "type": "number"
        },
        "program_name": {
          "title": "Program Name",
          "type": "string"
        },
        "rule_confidence": {
          "items": {
            "$ref": "#/$defs/RuleConfidence"
          },
          "title": "Rule Confidence",
          "type": "array"
        }
      },
      "required": [
        "program_name",
        "fidelity_issues",
        "rule_confidence",
        "overall_confidence"
      ],
      "title": "SpecCritiqueResult",
      "type": "object"
    },
    "SpecExtractionResult": {
      "description": "Everything `spec_extractor` produces for one program.\n\n`spec_markdown` is the model's narration; every other field is deterministic and independent\nof it -- a caller that only needs the field/paragraph facts (e.g. `spec_critic`, checking\nextraction quality) does not need to re-derive them from `spec_markdown` prose.",
      "properties": {
        "complexity": {
          "$ref": "#/$defs/ProgramComplexity"
        },
        "field_mappings": {
          "items": {
            "$ref": "#/$defs/PicMapping"
          },
          "title": "Field Mappings",
          "type": "array"
        },
        "injection_flags": {
          "items": {
            "$ref": "#/$defs/InjectionFlag"
          },
          "title": "Injection Flags",
          "type": "array"
        },
        "paragraph_names": {
          "items": {
            "type": "string"
          },
          "title": "Paragraph Names",
          "type": "array"
        },
        "program_name": {
          "title": "Program Name",
          "type": "string"
        },
        "spec_markdown": {
          "title": "Spec Markdown",
          "type": "string"
        },
        "unsupported_fields": {
          "items": {
            "$ref": "#/$defs/UnsupportedField"
          },
          "title": "Unsupported Fields",
          "type": "array"
        }
      },
      "required": [
        "program_name",
        "paragraph_names",
        "field_mappings",
        "unsupported_fields",
        "injection_flags",
        "spec_markdown",
        "complexity"
      ],
      "title": "SpecExtractionResult",
      "type": "object"
    },
    "UnifiedDesign": {
      "description": "`design.json`'s `unified_design` -- ADR-0010's real shape for what ADR-0008 left untyped.\n\n`domain_entities` is deterministic (see `nodes/solution_architect.build_domain_entities`);\n`batch_jobs` and `rest_endpoints` are LLM-authored, informed by but never re-deriving the\nentity data.",
      "properties": {
        "batch_jobs": {
          "items": {
            "$ref": "#/$defs/BatchJobDesign"
          },
          "title": "Batch Jobs",
          "type": "array"
        },
        "composite_types": {
          "items": {
            "$ref": "#/$defs/CompositeType"
          },
          "title": "Composite Types",
          "type": "array"
        },
        "domain_entities": {
          "items": {
            "$ref": "#/$defs/DomainEntity"
          },
          "title": "Domain Entities",
          "type": "array"
        },
        "file_access_paths": {
          "default": [],
          "items": {
            "$ref": "#/$defs/FileAccessPath"
          },
          "title": "File Access Paths",
          "type": "array"
        },
        "rest_endpoints": {
          "items": {
            "$ref": "#/$defs/RestEndpointDesign"
          },
          "title": "Rest Endpoints",
          "type": "array"
        }
      },
      "required": [
        "domain_entities",
        "batch_jobs",
        "rest_endpoints"
      ],
      "title": "UnifiedDesign",
      "type": "object"
    },
    "UnsupportedField": {
      "description": "One field declaration `pic_mapper` refused to map deterministically.\n\nPer ADR-0002, `REDEFINES`/`OCCURS ... DEPENDING ON` (`UnsupportedPicConstructError`) and any\n`PIC` clause `pic_mapper` cannot resolve without guessing (`ValueError`) are both caught here,\none field at a time -- see the module docstring. `reason` is the caught exception's own\nmessage, verbatim, so a human reviewing this as a gate item sees exactly why without\nre-deriving it themselves.",
      "properties": {
        "field_name": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "title": "Field Name"
        },
        "raw_text": {
          "title": "Raw Text",
          "type": "string"
        },
        "reason": {
          "title": "Reason",
          "type": "string"
        },
        "source_label": {
          "title": "Source Label",
          "type": "string"
        }
      },
      "required": [
        "source_label",
        "field_name",
        "raw_text",
        "reason"
      ],
      "title": "UnsupportedField",
      "type": "object"
    },
    "UsageClause": {
      "description": "COBOL `USAGE` clauses this module can identify.\n\n`DISPLAY` is COBOL's implicit default when no `USAGE` clause is present (zoned decimal for\nnumeric fields) -- it is not itself evidence that a `USAGE` clause was written.",
      "enum": [
        "DISPLAY",
        "COMP",
        "COMP-1",
        "COMP-2",
        "COMP-3",
        "BINARY"
      ],
      "title": "UsageClause",
      "type": "string"
    }
  },
  "description": "The `design.json` contract: everything control-plane's gate needs to review one `design`\ninvocation across every program it covered.\n\nBuild this via `build_design_document`, not by hand, so `gate_items` can never go stale\nrelative to `programs`. `unified_design` is `None` until `nodes/solution_architect.py` has\nactually run -- it's a real, typed `UnifiedDesign` (ADR-0010), not a placeholder dict.\n\n`cost` is `None` only when the document was built outside a `model_client.collect_usage`\nscope -- i.e. by a test constructing one directly. A real `run_design` always populates it.",
  "properties": {
    "cost": {
      "anyOf": [
        {
          "$ref": "#/$defs/RunCost"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    },
    "gate_items": {
      "items": {
        "$ref": "#/$defs/GateItem"
      },
      "title": "Gate Items",
      "type": "array"
    },
    "generated_at": {
      "format": "date-time",
      "title": "Generated At",
      "type": "string"
    },
    "programs": {
      "items": {
        "$ref": "#/$defs/ProgramDesignEntry"
      },
      "title": "Programs",
      "type": "array"
    },
    "schema_version": {
      "default": "3.9.0",
      "title": "Schema Version",
      "type": "string"
    },
    "unified_design": {
      "anyOf": [
        {
          "$ref": "#/$defs/UnifiedDesign"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    }
  },
  "required": [
    "generated_at",
    "programs",
    "gate_items"
  ],
  "title": "DesignDocument",
  "type": "object"
}
